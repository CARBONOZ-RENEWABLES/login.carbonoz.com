# mongod.conf

# Where to store data
storage:
  dbPath: /var/lib/mongodb

# Where to log
systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

# Network interfaces
net:
  port: 27017
  bindIp: 127.0.0.1

# Replica Set
replication:
  replSetName: "rs0"
